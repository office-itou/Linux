menuentry '[ Unattended installation... ]' {
	true
}

menuentry '- Debian 10 Net-install                                        2022-09-10 19:52:36' {
	set webprot="http"
	set webaddr="192.168.1.10"
	set webdirs="/pxeboot"
	set webroot="${webprot}://${webaddr}${webdirs}"
	set boottop="boot/debian-netinst-10"
	set isofile="debian-10.13.0-amd64-netinst.iso"
	set urlfile="url=${webroot}/isos/${isofile}"
	set dnsname="workgroup"
	set hstname="sv-debian"
	set hstfqdn="${hstname}.${dnsname}"
	set ip4addr="192.168.1.1"
	set ip4mask="255.255.255.0"
	set ip4gway="192.168.1.254"
	set ip4nsvr="192.168.1.254"
	set netconf="netcfg/disable_autoconfig=true netcfg/get_hostname=${hstfqdn} netcfg/get_ipaddress=${ip4addr} netcfg/get_gateway=${ip4gway} netcfg/get_nameservers=${ip4nsvr}"
	set netconf="hostname=${hstname} dnsdomain=${dnsname} ip=ens160:${ip4addr}:${ip4mask}:${ip4gway}:${ip4nsvr}"
	set preseed="auto=true preseed/url=${webroot}/conf/preseed/ps_debian-10_server.cfg"
	set locales="locales=C timezone=Asia/Tokyo keyboard-layouts=jp keyboard-model=jp106"
	set root="${webprot},${webaddr}"
	if [ "${grub_platform}" = "efi" ]; then rmmod tpm; fi
	echo "Loading ${isofile} ..."
	loopback loop (${root})${webdirs}/isos/${isofile}
	linuxefi   (loop)/install.amd/vmlinuz ${urlfile} ${preseed} ${netconf} ide=nodma fsck.mode=skip root=/boot toram ---
	initrdefi  (loop)/install.amd/initrd.gz
#	loopback --delete loop
}

menuentry '- Debian 11 Net-install                                        2023-10-07 16:07:32' {
	set webprot="http"
	set webaddr="192.168.1.10"
	set webdirs="/pxeboot"
	set webroot="${webprot}://${webaddr}${webdirs}"
	set boottop="boot/debian-netinst-11"
	set isofile="debian-11.8.0-amd64-netinst.iso"
	set urlfile="url=${webroot}/isos/${isofile}"
	set dnsname="workgroup"
	set hstname="sv-debian"
	set hstfqdn="${hstname}.${dnsname}"
	set ip4addr="192.168.1.1"
	set ip4mask="255.255.255.0"
	set ip4gway="192.168.1.254"
	set ip4nsvr="192.168.1.254"
	set netconf="netcfg/disable_autoconfig=true netcfg/get_hostname=${hstfqdn} netcfg/get_ipaddress=${ip4addr} netcfg/get_gateway=${ip4gway} netcfg/get_nameservers=${ip4nsvr}"
	set netconf="hostname=${hstname} dnsdomain=${dnsname} ip=ens160:${ip4addr}:${ip4mask}:${ip4gway}:${ip4nsvr}"
	set preseed="auto=true preseed/url=${webroot}/conf/preseed/ps_debian-11_server.cfg"
	set locales="locales=C timezone=Asia/Tokyo keyboard-layouts=jp keyboard-model=jp106"
	set root="${webprot},${webaddr}"
	if [ "${grub_platform}" = "efi" ]; then rmmod tpm; fi
	echo "Loading ${isofile} ..."
	loopback loop (${root})${webdirs}/isos/${isofile}
	linuxefi   (loop)/install.amd/vmlinuz ${urlfile} ${preseed} ${netconf} ide=nodma fsck.mode=skip root=/boot toram ---
	initrdefi  (loop)/install.amd/initrd.gz
#	loopback --delete loop
}

menuentry '- Debian 12 Net-install                                        2023-10-07 12:04:58' {
	set webprot="http"
	set webaddr="192.168.1.10"
	set webdirs="/pxeboot"
	set webroot="${webprot}://${webaddr}${webdirs}"
	set boottop="boot/debian-netinst-12"
	set isofile="debian-12.2.0-amd64-netinst.iso"
	set urlfile="url=${webroot}/isos/${isofile}"
	set dnsname="workgroup"
	set hstname="sv-debian"
	set hstfqdn="${hstname}.${dnsname}"
	set ip4addr="192.168.1.1"
	set ip4mask="255.255.255.0"
	set ip4gway="192.168.1.254"
	set ip4nsvr="192.168.1.254"
	set netconf="netcfg/disable_autoconfig=true netcfg/get_hostname=${hstfqdn} netcfg/get_ipaddress=${ip4addr} netcfg/get_gateway=${ip4gway} netcfg/get_nameservers=${ip4nsvr}"
	set netconf="hostname=${hstname} dnsdomain=${dnsname} ip=ens160:${ip4addr}:${ip4mask}:${ip4gway}:${ip4nsvr}"
	set preseed="auto=true preseed/url=${webroot}/conf/preseed/ps_debian-12_server.cfg"
	set locales="locales=C timezone=Asia/Tokyo keyboard-layouts=jp keyboard-model=jp106"
	set root="${webprot},${webaddr}"
	if [ "${grub_platform}" = "efi" ]; then rmmod tpm; fi
	echo "Loading ${isofile} ..."
	loopback loop (${root})${webdirs}/isos/${isofile}
	linuxefi   (loop)/install.amd/vmlinuz ${urlfile} ${preseed} ${netconf} ide=nodma fsck.mode=skip root=/boot toram ---
	initrdefi  (loop)/install.amd/initrd.gz
#	loopback --delete loop
}

menuentry '- Debian testing Net-install                                   2023-11-29 09:09:39' {
	set webprot="http"
	set webaddr="192.168.1.10"
	set webdirs="/pxeboot"
	set webroot="${webprot}://${webaddr}${webdirs}"
	set boottop="boot/debian-netinst-testing"
	set isofile="debian-testing-amd64-netinst.iso"
	set urlfile="url=${webroot}/isos/${isofile}"
	set dnsname="workgroup"
	set hstname="sv-debian"
	set hstfqdn="${hstname}.${dnsname}"
	set ip4addr="192.168.1.1"
	set ip4mask="255.255.255.0"
	set ip4gway="192.168.1.254"
	set ip4nsvr="192.168.1.254"
	set netconf="netcfg/disable_autoconfig=true netcfg/get_hostname=${hstfqdn} netcfg/get_ipaddress=${ip4addr} netcfg/get_gateway=${ip4gway} netcfg/get_nameservers=${ip4nsvr}"
	set netconf="hostname=${hstname} dnsdomain=${dnsname} ip=ens160:${ip4addr}:${ip4mask}:${ip4gway}:${ip4nsvr}"
	set preseed="auto=true preseed/url=${webroot}/conf/preseed/ps_debian-testing_server.cfg"
	set locales="locales=C timezone=Asia/Tokyo keyboard-layouts=jp keyboard-model=jp106"
	set root="${webprot},${webaddr}"
	if [ "${grub_platform}" = "efi" ]; then rmmod tpm; fi
	echo "Loading ${isofile} ..."
	loopback loop (${root})${webdirs}/isos/${isofile}
	linuxefi   (loop)/install.amd/vmlinuz ${urlfile} ${preseed} ${netconf} ide=nodma fsck.mode=skip root=/boot toram ---
	initrdefi  (loop)/install.amd/initrd.gz
#	loopback --delete loop
}

menuentry '- Ubuntu Server 18.04                                          2021-09-16 00:01:34' {
	set webprot="http"
	set webaddr="192.168.1.10"
	set webdirs="/pxeboot"
	set webroot="${webprot}://${webaddr}${webdirs}"
	set boottop="boot/ubuntu-server-18.04"
	set isofile="ubuntu-18.04.6-server-amd64.iso"
	set urlfile="url=${webroot}/isos/${isofile}"
	set dnsname="workgroup"
	set hstname="sv-ubuntu"
	set hstfqdn="${hstname}.${dnsname}"
	set ip4addr="192.168.1.1/24"
	set ip4gway="192.168.1.254"
	set ip4nsvr="192.168.1.254"
	set netconf="netcfg/disable_autoconfig=true netcfg/get_hostname=${hstfqdn} netcfg/get_ipaddress=${ip4addr} netcfg/get_gateway=${ip4gway} netcfg/get_nameservers=${ip4nsvr}"
	set netconf="hostname=${hstname} dnsdomain=${dnsname} ip=ens160:${ip4addr}:${ip4mask}:${ip4gway}:${ip4nsvr}:"
	set preseed="auto=true preseed/url=${webroot}/conf/preseed/ps_ubuntu-server-18.04.cfg"
	set locales="locales=C timezone=Asia/Tokyo keyboard-layouts=jp keyboard-model=jp106"
	set root="${webprot},${webaddr}"
	if [ "${grub_platform}" = "efi" ]; then rmmod tpm; fi
	echo "Loading ${isofile} ..."
	loopback loop (${root})${webdirs}/isos/${isofile}
	linuxefi   (loop)/install/vmlinuz ${urlfile} ${preseed} ${netconf} ide=nodma fsck.mode=skip root=/boot toram ---
	initrdefi  (loop)/install/initrd.gz
#	loopback --delete loop
}

menuentry '- Ubuntu Live Server 18.04                                     2021-09-15 20:42:12' {
	set webprot="http"
	set webaddr="192.168.1.10"
	set webdirs="/pxeboot"
	set webroot="${webprot}://${webaddr}${webdirs}"
	set boottop="boot/ubuntu-live-18.04"
	set isofile="ubuntu-18.04.6-live-server-amd64.iso"
	set urlfile="url=${webroot}/isos/${isofile}"
	set dnsname="workgroup"
	set hstname="sv-ubuntu"
	set hstfqdn="${hstname}.${dnsname}"
	set ip4addr="192.168.1.1"
	set ip4mask="255.255.255.0"
	set ip4gway="192.168.1.254"
	set ip4nsvr="192.168.1.254"
	set nocloud="automatic-ubiquity noprompt autoinstall ds=nocloud-net;s=${webroot}/conf/nocloud/ubuntu_old_server"
	set netconf="ip=${ip4addr}::${ip4gway}:${ip4mask}:${hstfqdn}:ens160:static:${ip4nsvr}"
	set locales="debian-installer/locale=ja_JP.UTF-8 timezone=Asia/Tokyo keyboard-configuration/layoutcode=jp keyboard-configuration/modelcode=jp106"
	set root="${webprot},${webaddr}"
	if [ "${grub_platform}" = "efi" ]; then rmmod tpm; fi
	echo "Loading ${isofile} ..."
	loopback loop (${root})${webdirs}/isos/${isofile}
	linuxefi   (loop)/casper/vmlinuz ${urlfile} ${nocloud} ${netconf} ide=nodma fsck.mode=skip root=/boot toram ---
	initrdefi  (loop)/casper/initrd
#	loopback --delete loop
}

menuentry '- Ubuntu Live Server 20.04                                     2023-03-14 23:02:35' {
	set webprot="http"
	set webaddr="192.168.1.10"
	set webdirs="/pxeboot"
	set webroot="${webprot}://${webaddr}${webdirs}"
	set boottop="boot/ubuntu-live-20.04"
	set isofile="ubuntu-20.04.6-live-server-amd64.iso"
	set urlfile="url=${webroot}/isos/${isofile}"
	set dnsname="workgroup"
	set hstname="sv-ubuntu"
	set hstfqdn="${hstname}.${dnsname}"
	set ip4addr="192.168.1.1"
	set ip4mask="255.255.255.0"
	set ip4gway="192.168.1.254"
	set ip4nsvr="192.168.1.254"
	set nocloud="automatic-ubiquity noprompt autoinstall ds=nocloud-net;s=${webroot}/conf/nocloud/ubuntu_server"
	set netconf="ip=${ip4addr}::${ip4gway}:${ip4mask}:${hstfqdn}:ens160:static:${ip4nsvr}"
	set locales="debian-installer/locale=ja_JP.UTF-8 timezone=Asia/Tokyo keyboard-configuration/layoutcode=jp keyboard-configuration/modelcode=jp106"
	set root="${webprot},${webaddr}"
	if [ "${grub_platform}" = "efi" ]; then rmmod tpm; fi
	echo "Loading ${isofile} ..."
	loopback loop (${root})${webdirs}/isos/${isofile}
	linuxefi   (loop)/casper/vmlinuz ${urlfile} ${nocloud} ${netconf} ide=nodma fsck.mode=skip root=/boot toram ---
	initrdefi  (loop)/casper/initrd
#	loopback --delete loop
}

menuentry '- Ubuntu Live Server 22.04                                     2023-08-10 05:06:27' {
	set webprot="http"
	set webaddr="192.168.1.10"
	set webdirs="/pxeboot"
	set webroot="${webprot}://${webaddr}${webdirs}"
	set boottop="boot/ubuntu-live-22.04"
	set isofile="ubuntu-22.04.3-live-server-amd64.iso"
	set urlfile="url=${webroot}/isos/${isofile}"
	set dnsname="workgroup"
	set hstname="sv-ubuntu"
	set hstfqdn="${hstname}.${dnsname}"
	set ip4addr="192.168.1.1"
	set ip4mask="255.255.255.0"
	set ip4gway="192.168.1.254"
	set ip4nsvr="192.168.1.254"
	set nocloud="automatic-ubiquity noprompt autoinstall ds=nocloud-net;s=${webroot}/conf/nocloud/ubuntu_server"
	set netconf="ip=${ip4addr}::${ip4gway}:${ip4mask}:${hstfqdn}:ens160:static:${ip4nsvr}"
	set locales="debian-installer/locale=ja_JP.UTF-8 timezone=Asia/Tokyo keyboard-configuration/layoutcode=jp keyboard-configuration/modelcode=jp106"
	set root="${webprot},${webaddr}"
	if [ "${grub_platform}" = "efi" ]; then rmmod tpm; fi
	echo "Loading ${isofile} ..."
	loopback loop (${root})${webdirs}/isos/${isofile}
	linuxefi   (loop)/casper/vmlinuz ${urlfile} ${nocloud} ${netconf} ide=nodma fsck.mode=skip root=/boot toram ---
	initrdefi  (loop)/casper/initrd
#	loopback --delete loop
}

menuentry '- Ubuntu Live Server 23.04                                     2023-04-15 20:00:30' {
	set webprot="http"
	set webaddr="192.168.1.10"
	set webdirs="/pxeboot"
	set webroot="${webprot}://${webaddr}${webdirs}"
	set boottop="boot/ubuntu-live-23.04"
	set isofile="ubuntu-23.04-live-server-amd64.iso"
	set urlfile="url=${webroot}/isos/${isofile}"
	set dnsname="workgroup"
	set hstname="sv-ubuntu"
	set hstfqdn="${hstname}.${dnsname}"
	set ip4addr="192.168.1.1"
	set ip4mask="255.255.255.0"
	set ip4gway="192.168.1.254"
	set ip4nsvr="192.168.1.254"
	set nocloud="automatic-ubiquity noprompt autoinstall ds=nocloud-net;s=${webroot}/conf/nocloud/ubuntu_server"
	set netconf="ip=${ip4addr}::${ip4gway}:${ip4mask}:${hstfqdn}:ens160:static:${ip4nsvr}"
	set locales="debian-installer/locale=ja_JP.UTF-8 timezone=Asia/Tokyo keyboard-configuration/layoutcode=jp keyboard-configuration/modelcode=jp106"
	set root="${webprot},${webaddr}"
	if [ "${grub_platform}" = "efi" ]; then rmmod tpm; fi
	echo "Loading ${isofile} ..."
	loopback loop (${root})${webdirs}/isos/${isofile}
	linuxefi   (loop)/casper/vmlinuz ${urlfile} ${nocloud} ${netconf} ide=nodma fsck.mode=skip root=/boot toram ---
	initrdefi  (loop)/casper/initrd
#	loopback --delete loop
}

menuentry '- Ubuntu Live Server 23.10                                     2023-10-11 04:52:01' {
	set webprot="http"
	set webaddr="192.168.1.10"
	set webdirs="/pxeboot"
	set webroot="${webprot}://${webaddr}${webdirs}"
	set boottop="boot/ubuntu-live-23.10"
	set isofile="ubuntu-23.10-live-server-amd64.iso"
	set urlfile="url=${webroot}/isos/${isofile}"
	set dnsname="workgroup"
	set hstname="sv-ubuntu"
	set hstfqdn="${hstname}.${dnsname}"
	set ip4addr="192.168.1.1"
	set ip4mask="255.255.255.0"
	set ip4gway="192.168.1.254"
	set ip4nsvr="192.168.1.254"
	set nocloud="automatic-ubiquity noprompt autoinstall ds=nocloud-net;s=${webroot}/conf/nocloud/ubuntu_server"
	set netconf="ip=${ip4addr}::${ip4gway}:${ip4mask}:${hstfqdn}:ens160:static:${ip4nsvr}"
	set locales="debian-installer/locale=ja_JP.UTF-8 timezone=Asia/Tokyo keyboard-configuration/layoutcode=jp keyboard-configuration/modelcode=jp106"
	set root="${webprot},${webaddr}"
	if [ "${grub_platform}" = "efi" ]; then rmmod tpm; fi
	echo "Loading ${isofile} ..."
	loopback loop (${root})${webdirs}/isos/${isofile}
	linuxefi   (loop)/casper/vmlinuz ${urlfile} ${nocloud} ${netconf} ide=nodma fsck.mode=skip root=/boot toram ---
	initrdefi  (loop)/casper/initrd
#	loopback --delete loop
}

menuentry '- Ubuntu Live Server noble                                     2023-11-27 06:31:14' {
	set webprot="http"
	set webaddr="192.168.1.10"
	set webdirs="/pxeboot"
	set webroot="${webprot}://${webaddr}${webdirs}"
	set boottop="boot/ubuntu-live-noble"
	set isofile="noble-live-server-amd64.iso"
	set urlfile="url=${webroot}/isos/${isofile}"
	set dnsname="workgroup"
	set hstname="sv-ubuntu"
	set hstfqdn="${hstname}.${dnsname}"
	set ip4addr="192.168.1.1"
	set ip4mask="255.255.255.0"
	set ip4gway="192.168.1.254"
	set ip4nsvr="192.168.1.254"
	set nocloud="automatic-ubiquity noprompt autoinstall ds=nocloud-net;s=${webroot}/conf/nocloud/ubuntu_server"
	set netconf="ip=${ip4addr}::${ip4gway}:${ip4mask}:${hstfqdn}:ens160:static:${ip4nsvr}"
	set locales="debian-installer/locale=ja_JP.UTF-8 timezone=Asia/Tokyo keyboard-configuration/layoutcode=jp keyboard-configuration/modelcode=jp106"
	set root="${webprot},${webaddr}"
	if [ "${grub_platform}" = "efi" ]; then rmmod tpm; fi
	echo "Loading ${isofile} ..."
	loopback loop (${root})${webdirs}/isos/${isofile}
	linuxefi   (loop)/casper/vmlinuz ${urlfile} ${nocloud} ${netconf} ide=nodma fsck.mode=skip root=/boot toram ---
	initrdefi  (loop)/casper/initrd
#	loopback --delete loop
}

menuentry '- Fedora Server 38                                             2023-04-13 21:39:53' {
	set webprot="http"
	set webaddr="192.168.1.10"
	set webdirs="/pxeboot"
	set webroot="${webprot}://${webaddr}${webdirs}"
	set boottop="boot/fedora-38"
	set isofile="Fedora-Server-dvd-x86_64-38-1.6.iso"
	set urlfile="inst.repo=${webroot}/imgs/fedora-38"
	set dnsname="workgroup"
	set hstname="sv-fedora"
	set hstfqdn="${hstname}.${dnsname}"
	set ip4addr="192.168.1.1"
	set ip4mask="255.255.255.0"
	set ip4gway="192.168.1.254"
	set ip4nsvr="192.168.1.254"
	set ksstart="inst.ks=${webroot}/conf/kickstart/ks_fedora-38.cfg"
	set netconf="ip=${ip4addr}::${ip4gway}:${ip4mask}:${hstfqdn}:ens160:none,auto6 nameserver=${ip4nsvr}"
	set locales="locale=C timezone=Asia/Tokyo keyboard-configuration/layoutcode=jp keyboard-configuration/modelcode=jp106"
	set root="${webprot},${webaddr}"
	if [ "${grub_platform}" = "efi" ]; then rmmod tpm; fi
	echo "Loading ${isofile} ..."
	loopback loop (${root})${webdirs}/isos/${isofile}
	probe --label --set=hdlabel (loop)
	linuxefi   (loop)/images/pxeboot/vmlinuz inst.repo=(${root})${webdirs}/isos/${isofile} ${ksstart} ${netconf} fsck.mode=skip ---
	initrdefi  (loop)/images/pxeboot/initrd.img
#	loopback --delete loop
}

menuentry '- Fedora Server 39                                             2023-11-01 01:01:16' {
	set webprot="http"
	set webaddr="192.168.1.10"
	set webdirs="/pxeboot"
	set webroot="${webprot}://${webaddr}${webdirs}"
	set boottop="boot/fedora-39"
	set isofile="Fedora-Server-dvd-x86_64-39-1.5.iso"
	set urlfile="inst.repo=${webroot}/imgs/fedora-39"
	set dnsname="workgroup"
	set hstname="sv-fedora"
	set hstfqdn="${hstname}.${dnsname}"
	set ip4addr="192.168.1.1"
	set ip4mask="255.255.255.0"
	set ip4gway="192.168.1.254"
	set ip4nsvr="192.168.1.254"
	set ksstart="inst.ks=${webroot}/conf/kickstart/ks_fedora-39.cfg"
	set netconf="ip=${ip4addr}::${ip4gway}:${ip4mask}:${hstfqdn}:ens160:none,auto6 nameserver=${ip4nsvr}"
	set locales="locale=C timezone=Asia/Tokyo keyboard-configuration/layoutcode=jp keyboard-configuration/modelcode=jp106"
	set root="${webprot},${webaddr}"
	if [ "${grub_platform}" = "efi" ]; then rmmod tpm; fi
	echo "Loading ${isofile} ..."
	loopback loop (${root})${webdirs}/isos/${isofile}
	probe --label --set=hdlabel (loop)
	linuxefi   (loop)/images/pxeboot/vmlinuz inst.repo=(${root})${webdirs}/isos/${isofile} ${ksstart} ${netconf} fsck.mode=skip ---
	initrdefi  (loop)/images/pxeboot/initrd.img
#	loopback --delete loop
}

menuentry '- CentOS Stream 8                                              2023-11-27 05:17:03' {
	set webprot="http"
	set webaddr="192.168.1.10"
	set webdirs="/pxeboot"
	set webroot="${webprot}://${webaddr}${webdirs}"
	set boottop="boot/centos-stream-8"
	set isofile="CentOS-Stream-8-x86_64-latest-dvd1.iso"
	set urlfile="inst.repo=${webroot}/imgs/centos-stream-8"
	set dnsname="workgroup"
	set hstname="sv-centos"
	set hstfqdn="${hstname}.${dnsname}"
	set ip4addr="192.168.1.1"
	set ip4mask="255.255.255.0"
	set ip4gway="192.168.1.254"
	set ip4nsvr="192.168.1.254"
	set ksstart="inst.ks=${webroot}/conf/kickstart/ks_centos-stream-8.cfg"
	set netconf="ip=${ip4addr}::${ip4gway}:${ip4mask}:${hstfqdn}:ens160:none,auto6 nameserver=${ip4nsvr}"
	set locales="locale=C timezone=Asia/Tokyo keyboard-configuration/layoutcode=jp keyboard-configuration/modelcode=jp106"
	set root="${webprot},${webaddr}"
	if [ "${grub_platform}" = "efi" ]; then rmmod tpm; fi
	echo "Loading ${isofile} ..."
	loopback loop (${root})${webdirs}/isos/${isofile}
	probe --label --set=hdlabel (loop)
	linuxefi   (loop)/images/pxeboot/vmlinuz inst.repo=(${root})${webdirs}/isos/${isofile} ${ksstart} ${netconf} fsck.mode=skip ---
	initrdefi  (loop)/images/pxeboot/initrd.img
#	loopback --delete loop
}

menuentry '- CentOS Stream 9                                              2023-11-28 15:19:54' {
	set webprot="http"
	set webaddr="192.168.1.10"
	set webdirs="/pxeboot"
	set webroot="${webprot}://${webaddr}${webdirs}"
	set boottop="boot/centos-stream-9"
	set isofile="CentOS-Stream-9-latest-x86_64-dvd1.iso"
	set urlfile="inst.repo=${webroot}/imgs/centos-stream-9"
	set dnsname="workgroup"
	set hstname="sv-centos"
	set hstfqdn="${hstname}.${dnsname}"
	set ip4addr="192.168.1.1"
	set ip4mask="255.255.255.0"
	set ip4gway="192.168.1.254"
	set ip4nsvr="192.168.1.254"
	set ksstart="inst.ks=${webroot}/conf/kickstart/ks_centos-stream-9.cfg"
	set netconf="ip=${ip4addr}::${ip4gway}:${ip4mask}:${hstfqdn}:ens160:none,auto6 nameserver=${ip4nsvr}"
	set locales="locale=C timezone=Asia/Tokyo keyboard-configuration/layoutcode=jp keyboard-configuration/modelcode=jp106"
	set root="${webprot},${webaddr}"
	if [ "${grub_platform}" = "efi" ]; then rmmod tpm; fi
	echo "Loading ${isofile} ..."
	loopback loop (${root})${webdirs}/isos/${isofile}
	probe --label --set=hdlabel (loop)
	linuxefi   (loop)/images/pxeboot/vmlinuz inst.repo=(${root})${webdirs}/isos/${isofile} ${ksstart} ${netconf} fsck.mode=skip ---
	initrdefi  (loop)/images/pxeboot/initrd.img
#	loopback --delete loop
}

menuentry '- Alma Linux 9                                                 2023-11-10 21:00:28' {
	set webprot="http"
	set webaddr="192.168.1.10"
	set webdirs="/pxeboot"
	set webroot="${webprot}://${webaddr}${webdirs}"
	set boottop="boot/almalinux-9"
	set isofile="AlmaLinux-9-latest-x86_64-dvd.iso"
	set urlfile="inst.repo=${webroot}/imgs/almalinux-9"
	set dnsname="workgroup"
	set hstname="sv-almalinux"
	set hstfqdn="${hstname}.${dnsname}"
	set ip4addr="192.168.1.1"
	set ip4mask="255.255.255.0"
	set ip4gway="192.168.1.254"
	set ip4nsvr="192.168.1.254"
	set ksstart="inst.ks=${webroot}/conf/kickstart/ks_almalinux-9.cfg"
	set netconf="ip=${ip4addr}::${ip4gway}:${ip4mask}:${hstfqdn}:ens160:none,auto6 nameserver=${ip4nsvr}"
	set locales="locale=C timezone=Asia/Tokyo keyboard-configuration/layoutcode=jp keyboard-configuration/modelcode=jp106"
	set root="${webprot},${webaddr}"
	if [ "${grub_platform}" = "efi" ]; then rmmod tpm; fi
	echo "Loading ${isofile} ..."
	loopback loop (${root})${webdirs}/isos/${isofile}
	probe --label --set=hdlabel (loop)
	linuxefi   (loop)/images/pxeboot/vmlinuz inst.repo=(${root})${webdirs}/isos/${isofile} ${ksstart} ${netconf} fsck.mode=skip ---
	initrdefi  (loop)/images/pxeboot/initrd.img
#	loopback --delete loop
}

menuentry '- Rocky Linux 8                                                2023-05-17 23:45:08' {
	set webprot="http"
	set webaddr="192.168.1.10"
	set webdirs="/pxeboot"
	set webroot="${webprot}://${webaddr}${webdirs}"
	set boottop="boot/rockylinux-8"
	set isofile="Rocky-8.8-x86_64-dvd1.iso"
	set urlfile="inst.repo=${webroot}/imgs/rockylinux-8"
	set dnsname="workgroup"
	set hstname="sv-rockylinux"
	set hstfqdn="${hstname}.${dnsname}"
	set ip4addr="192.168.1.1"
	set ip4mask="255.255.255.0"
	set ip4gway="192.168.1.254"
	set ip4nsvr="192.168.1.254"
	set ksstart="inst.ks=${webroot}/conf/kickstart/ks_rockylinux-8.cfg"
	set netconf="ip=${ip4addr}::${ip4gway}:${ip4mask}:${hstfqdn}:ens160:none,auto6 nameserver=${ip4nsvr}"
	set locales="locale=C timezone=Asia/Tokyo keyboard-configuration/layoutcode=jp keyboard-configuration/modelcode=jp106"
	set root="${webprot},${webaddr}"
	if [ "${grub_platform}" = "efi" ]; then rmmod tpm; fi
	echo "Loading ${isofile} ..."
	loopback loop (${root})${webdirs}/isos/${isofile}
	probe --label --set=hdlabel (loop)
	linuxefi   (loop)/images/pxeboot/vmlinuz inst.repo=(${root})${webdirs}/isos/${isofile} ${ksstart} ${netconf} fsck.mode=skip ---
	initrdefi  (loop)/images/pxeboot/initrd.img
#	loopback --delete loop
}

menuentry '- Rocky Linux 9                                                2023-11-12 22:51:53' {
	set webprot="http"
	set webaddr="192.168.1.10"
	set webdirs="/pxeboot"
	set webroot="${webprot}://${webaddr}${webdirs}"
	set boottop="boot/rockylinux-9"
	set isofile="Rocky-9-latest-x86_64-dvd.iso"
	set urlfile="inst.repo=${webroot}/imgs/rockylinux-9"
	set dnsname="workgroup"
	set hstname="sv-rockylinux"
	set hstfqdn="${hstname}.${dnsname}"
	set ip4addr="192.168.1.1"
	set ip4mask="255.255.255.0"
	set ip4gway="192.168.1.254"
	set ip4nsvr="192.168.1.254"
	set ksstart="inst.ks=${webroot}/conf/kickstart/ks_rockylinux-9.cfg"
	set netconf="ip=${ip4addr}::${ip4gway}:${ip4mask}:${hstfqdn}:ens160:none,auto6 nameserver=${ip4nsvr}"
	set locales="locale=C timezone=Asia/Tokyo keyboard-configuration/layoutcode=jp keyboard-configuration/modelcode=jp106"
	set root="${webprot},${webaddr}"
	if [ "${grub_platform}" = "efi" ]; then rmmod tpm; fi
	echo "Loading ${isofile} ..."
	loopback loop (${root})${webdirs}/isos/${isofile}
	probe --label --set=hdlabel (loop)
	linuxefi   (loop)/images/pxeboot/vmlinuz inst.repo=(${root})${webdirs}/isos/${isofile} ${ksstart} ${netconf} fsck.mode=skip ---
	initrdefi  (loop)/images/pxeboot/initrd.img
#	loopback --delete loop
}

menuentry '- Miracle Linux 8                                              2023-10-04 10:06:21' {
	set webprot="http"
	set webaddr="192.168.1.10"
	set webdirs="/pxeboot"
	set webroot="${webprot}://${webaddr}${webdirs}"
	set boottop="boot/miraclelinux-8"
	set isofile="MIRACLELINUX-8.8-rtm-x86_64.iso"
	set urlfile="inst.repo=${webroot}/imgs/miraclelinux-8"
	set dnsname="workgroup"
	set hstname="sv-miraclelinux"
	set hstfqdn="${hstname}.${dnsname}"
	set ip4addr="192.168.1.1"
	set ip4mask="255.255.255.0"
	set ip4gway="192.168.1.254"
	set ip4nsvr="192.168.1.254"
	set ksstart="inst.ks=${webroot}/conf/kickstart/ks_miraclelinux-8.cfg"
	set netconf="ip=${ip4addr}::${ip4gway}:${ip4mask}:${hstfqdn}:ens160:none,auto6 nameserver=${ip4nsvr}"
	set locales="locale=C timezone=Asia/Tokyo keyboard-configuration/layoutcode=jp keyboard-configuration/modelcode=jp106"
	set root="${webprot},${webaddr}"
	if [ "${grub_platform}" = "efi" ]; then rmmod tpm; fi
	echo "Loading ${isofile} ..."
	loopback loop (${root})${webdirs}/isos/${isofile}
	probe --label --set=hdlabel (loop)
	linuxefi   (loop)/images/pxeboot/vmlinuz inst.repo=(${root})${webdirs}/isos/${isofile} ${ksstart} ${netconf} fsck.mode=skip ---
	initrdefi  (loop)/images/pxeboot/initrd.img
#	loopback --delete loop
}

menuentry '- Miracle Linux 9                                              2023-10-04 09:37:20' {
	set webprot="http"
	set webaddr="192.168.1.10"
	set webdirs="/pxeboot"
	set webroot="${webprot}://${webaddr}${webdirs}"
	set boottop="boot/miraclelinux-9"
	set isofile="MIRACLELINUX-9.2-rtm-x86_64.iso"
	set urlfile="inst.repo=${webroot}/imgs/miraclelinux-9"
	set dnsname="workgroup"
	set hstname="sv-miraclelinux"
	set hstfqdn="${hstname}.${dnsname}"
	set ip4addr="192.168.1.1"
	set ip4mask="255.255.255.0"
	set ip4gway="192.168.1.254"
	set ip4nsvr="192.168.1.254"
	set ksstart="inst.ks=${webroot}/conf/kickstart/ks_miraclelinux-9.cfg"
	set netconf="ip=${ip4addr}::${ip4gway}:${ip4mask}:${hstfqdn}:ens160:none,auto6 nameserver=${ip4nsvr}"
	set locales="locale=C timezone=Asia/Tokyo keyboard-configuration/layoutcode=jp keyboard-configuration/modelcode=jp106"
	set root="${webprot},${webaddr}"
	if [ "${grub_platform}" = "efi" ]; then rmmod tpm; fi
	echo "Loading ${isofile} ..."
	loopback loop (${root})${webdirs}/isos/${isofile}
	probe --label --set=hdlabel (loop)
	linuxefi   (loop)/images/pxeboot/vmlinuz inst.repo=(${root})${webdirs}/isos/${isofile} ${ksstart} ${netconf} fsck.mode=skip ---
	initrdefi  (loop)/images/pxeboot/initrd.img
#	loopback --delete loop
}

menuentry '- openSUSE Leap 15.5                                           2023-05-23 15:21:52' {
	set webprot="http"
	set webaddr="192.168.1.10"
	set webdirs="/pxeboot"
	set webroot="${webprot}://${webaddr}${webdirs}"
	set boottop="boot/opensuse-leap-15.5"
	set isofile="openSUSE-Leap-15.5-DVD-x86_64-Media.iso"
	set urlfile="install=${webroot}/imgs/opensuse-leap-15.5"
	set dnsname="workgroup"
	set hstname="sv-opensuse"
	set hstfqdn="${hstname}.${dnsname}"
	set wkgroup="workgroup"
	set ip4addr="192.168.1.1/24"
	set ip4gway="192.168.1.254"
	set ip4nsvr="192.168.1.254"
	set autoxml="autoyast=${webroot}/conf/autoyast/autoinst_leap_15.5.xml"
	set netconf="hostname=${hstfqdn} ifcfg=e*=${ip4addr},${ip4gway},${ip4nsvr},${wkgroup}"
	set locales="locale=C timezone=Asia/Tokyo keyboard-configuration/layoutcode=jp keyboard-configuration/modelcode=jp106"
	set root="${webprot},${webaddr}"
	if [ "${grub_platform}" = "efi" ]; then rmmod tpm; fi
	echo "Loading ${isofile} ..."
	loopback loop (${root})${webdirs}/isos/${isofile}
	probe --label --set=hdlabel (loop)
	linuxefi   (loop)/boot/x86_64/loader/linux ${urlfile} ${autoxml} ${netconf} fsck.mode=skip ---
	initrdefi  (loop)/boot/x86_64/loader/initrd
#	loopback --delete loop
}

menuentry '- openSUSE Leap 15.6                                           2023-11-19 01:30:20' {
	set webprot="http"
	set webaddr="192.168.1.10"
	set webdirs="/pxeboot"
	set webroot="${webprot}://${webaddr}${webdirs}"
	set boottop="boot/opensuse-leap-15.6"
	set isofile="openSUSE-Leap-15.6-DVD-x86_64-Media.iso"
	set urlfile="install=${webroot}/imgs/opensuse-leap-15.6"
	set dnsname="workgroup"
	set hstname="sv-opensuse"
	set hstfqdn="${hstname}.${dnsname}"
	set wkgroup="workgroup"
	set ip4addr="192.168.1.1/24"
	set ip4gway="192.168.1.254"
	set ip4nsvr="192.168.1.254"
	set autoxml="autoyast=${webroot}/conf/autoyast/autoinst_leap_15.6.xml"
	set netconf="hostname=${hstfqdn} ifcfg=e*=${ip4addr},${ip4gway},${ip4nsvr},${wkgroup}"
	set locales="locale=C timezone=Asia/Tokyo keyboard-configuration/layoutcode=jp keyboard-configuration/modelcode=jp106"
	set root="${webprot},${webaddr}"
	if [ "${grub_platform}" = "efi" ]; then rmmod tpm; fi
	echo "Loading ${isofile} ..."
	loopback loop (${root})${webdirs}/isos/${isofile}
	probe --label --set=hdlabel (loop)
	linuxefi   (loop)/boot/x86_64/loader/linux ${urlfile} ${autoxml} ${netconf} fsck.mode=skip ---
	initrdefi  (loop)/boot/x86_64/loader/initrd
#	loopback --delete loop
}

menuentry '- openSUSE Tumbleweed                                          2023-11-28 01:28:23' {
	set webprot="http"
	set webaddr="192.168.1.10"
	set webdirs="/pxeboot"
	set webroot="${webprot}://${webaddr}${webdirs}"
	set boottop="boot/opensuse-tumbleweed"
	set isofile="openSUSE-Tumbleweed-DVD-x86_64-Current.iso"
	set urlfile="install=${webroot}/imgs/opensuse-tumbleweed"
	set dnsname="workgroup"
	set hstname="sv-opensuse"
	set hstfqdn="${hstname}.${dnsname}"
	set wkgroup="workgroup"
	set ip4addr="192.168.1.1/24"
	set ip4gway="192.168.1.254"
	set ip4nsvr="192.168.1.254"
	set autoxml="autoyast=${webroot}/conf/autoyast/autoinst_tumbleweed.xml"
	set netconf="hostname=${hstfqdn} ifcfg=e*=${ip4addr},${ip4gway},${ip4nsvr},${wkgroup}"
	set locales="locale=C timezone=Asia/Tokyo keyboard-configuration/layoutcode=jp keyboard-configuration/modelcode=jp106"
	set root="${webprot},${webaddr}"
	if [ "${grub_platform}" = "efi" ]; then rmmod tpm; fi
	echo "Loading ${isofile} ..."
	loopback loop (${root})${webdirs}/isos/${isofile}
	probe --label --set=hdlabel (loop)
	linuxefi   (loop)/boot/x86_64/loader/linux ${urlfile} ${autoxml} ${netconf} fsck.mode=skip ---
	initrdefi  (loop)/boot/x86_64/loader/initrd
#	loopback --delete loop
}

menuentry '[ Live media... ]' {
	true
}

menuentry '- Debian Live 10                                               2022-09-10 19:01:33' {
	set webprot="http"
	set webaddr="192.168.1.10"
	set webdirs="/pxeboot"
	set webroot="${webprot}://${webaddr}${webdirs}"
	set isofile="debian-live-10.13.0-amd64-lxde.iso"
	set urlfile="fetch=${webroot}/isos/${isofile}"
	set locales="locales=ja_JP.UTF-8 timezone=Asia/Tokyo keyboard-layouts=jp keyboard-model=jp106"
	set root="${webprot},${webaddr}"
	if [ "${grub_platform}" = "efi" ]; then rmmod tpm; fi
	echo "Loading ${isofile} ..."
	loopback loop (${root})${webdirs}/isos/${isofile}
	linuxefi   (loop)/live/vmlinuz-4.19.0-21-amd64 ${urlfile} ${locales} ip=dhcp ide=nodma fsck.mode=skip root=/boot boot=live toram=filesystem.squashfs ---
	initrdefi  (loop)/live/initrd.img-4.19.0-21-amd64
#	loopback --delete loop
}

menuentry '- Debian Live 11                                               2023-10-07 21:12:04' {
	set webprot="http"
	set webaddr="192.168.1.10"
	set webdirs="/pxeboot"
	set webroot="${webprot}://${webaddr}${webdirs}"
	set isofile="debian-live-11.8.0-amd64-lxde.iso"
	set urlfile="fetch=${webroot}/isos/${isofile}"
	set locales="locales=ja_JP.UTF-8 timezone=Asia/Tokyo keyboard-layouts=jp keyboard-model=jp106"
	set root="${webprot},${webaddr}"
	if [ "${grub_platform}" = "efi" ]; then rmmod tpm; fi
	echo "Loading ${isofile} ..."
	loopback loop (${root})${webdirs}/isos/${isofile}
	linuxefi   (loop)/live/vmlinuz-5.10.0-26-amd64 ${urlfile} ${locales} ip=dhcp ide=nodma fsck.mode=skip root=/boot boot=live toram=filesystem.squashfs ---
	initrdefi  (loop)/live/initrd.img-5.10.0-26-amd64
#	loopback --delete loop
}

menuentry '- Debian Live 12                                               2023-10-07 09:29:19' {
	set webprot="http"
	set webaddr="192.168.1.10"
	set webdirs="/pxeboot"
	set webroot="${webprot}://${webaddr}${webdirs}"
	set isofile="debian-live-12.2.0-amd64-lxde.iso"
	set urlfile="fetch=${webroot}/isos/${isofile}"
	set locales="locales=ja_JP.UTF-8 timezone=Asia/Tokyo keyboard-layouts=jp keyboard-model=jp106"
	set root="${webprot},${webaddr}"
	if [ "${grub_platform}" = "efi" ]; then rmmod tpm; fi
	echo "Loading ${isofile} ..."
	loopback loop (${root})${webdirs}/isos/${isofile}
	linuxefi   (loop)/live/vmlinuz ${urlfile} ${locales} ip=dhcp ide=nodma fsck.mode=skip root=/boot boot=live toram=filesystem.squashfs ---
	initrdefi  (loop)/live/initrd.img
#	loopback --delete loop
}

menuentry '- Debian Live testing                                          2023-11-27 02:12:03' {
	set webprot="http"
	set webaddr="192.168.1.10"
	set webdirs="/pxeboot"
	set webroot="${webprot}://${webaddr}${webdirs}"
	set isofile="debian-live-testing-amd64-lxde.iso"
	set urlfile="fetch=${webroot}/isos/${isofile}"
	set locales="locales=ja_JP.UTF-8 timezone=Asia/Tokyo keyboard-layouts=jp keyboard-model=jp106"
	set root="${webprot},${webaddr}"
	if [ "${grub_platform}" = "efi" ]; then rmmod tpm; fi
	echo "Loading ${isofile} ..."
	loopback loop (${root})${webdirs}/isos/${isofile}
	linuxefi   (loop)/live/vmlinuz ${urlfile} ${locales} ip=dhcp ide=nodma fsck.mode=skip root=/boot boot=live toram=filesystem.squashfs ---
	initrdefi  (loop)/live/initrd.img
#	loopback --delete loop
}

menuentry '- Ubuntu Desktop 18.04                                         2021-09-15 20:42:31' {
	set webprot="http"
	set webaddr="192.168.1.10"
	set webdirs="/pxeboot"
	set webroot="${webprot}://${webaddr}${webdirs}"
	set isofile="ubuntu-18.04.6-desktop-amd64.iso"
	set urlfile="url=${webroot}/isos/${isofile}"
	set locales="debian-installer/locale=ja_JP.UTF-8 keyboard-configuration/layoutcode=jp keyboard-configuration/modelcode=jp106"
	set root="${webprot},${webaddr}"
	if [ "${grub_platform}" = "efi" ]; then rmmod tpm; fi
	echo "Loading ${isofile} ..."
	loopback loop (${root})${webdirs}/isos/${isofile}
	linuxefi   (loop)/casper/vmlinuz ${urlfile} ${locales} ip=dhcp ide=nodma fsck.mode=skip root=/boot boot=casper toram=filesystem.squashfs maybe-ubiquity ---
	initrdefi  (loop)/casper/initrd
#	loopback --delete loop
}

menuentry '- Ubuntu Desktop 20.04                                         2023-03-16 15:58:09' {
	set webprot="http"
	set webaddr="192.168.1.10"
	set webdirs="/pxeboot"
	set webroot="${webprot}://${webaddr}${webdirs}"
	set isofile="ubuntu-20.04.6-desktop-amd64.iso"
	set urlfile="url=${webroot}/isos/${isofile}"
	set locales="debian-installer/locale=ja_JP.UTF-8 keyboard-configuration/layoutcode=jp keyboard-configuration/modelcode=jp106"
	set root="${webprot},${webaddr}"
	if [ "${grub_platform}" = "efi" ]; then rmmod tpm; fi
	echo "Loading ${isofile} ..."
	loopback loop (${root})${webdirs}/isos/${isofile}
	linuxefi   (loop)/casper/vmlinuz ${urlfile} ${locales} ip=dhcp ide=nodma fsck.mode=skip root=/boot boot=casper toram=filesystem.squashfs maybe-ubiquity ---
	initrdefi  (loop)/casper/initrd
#	loopback --delete loop
}

menuentry '- Ubuntu Desktop 22.04                                         2023-08-08 01:19:36' {
	set webprot="http"
	set webaddr="192.168.1.10"
	set webdirs="/pxeboot"
	set webroot="${webprot}://${webaddr}${webdirs}"
	set isofile="ubuntu-22.04.3-desktop-amd64.iso"
	set urlfile="url=${webroot}/isos/${isofile}"
	set locales="debian-installer/locale=ja_JP.UTF-8 keyboard-configuration/layoutcode=jp keyboard-configuration/modelcode=jp106"
	set root="${webprot},${webaddr}"
	if [ "${grub_platform}" = "efi" ]; then rmmod tpm; fi
	echo "Loading ${isofile} ..."
	loopback loop (${root})${webdirs}/isos/${isofile}
	linuxefi   (loop)/casper/vmlinuz ${urlfile} ${locales} ip=dhcp ide=nodma fsck.mode=skip root=/boot boot=casper toram=filesystem.squashfs maybe-ubiquity ---
	initrdefi  (loop)/casper/initrd
#	loopback --delete loop
}

menuentry '- Ubuntu Desktop 23.04                                         2023-04-18 21:06:08' {
	set webprot="http"
	set webaddr="192.168.1.10"
	set webdirs="/pxeboot"
	set webroot="${webprot}://${webaddr}${webdirs}"
	set isofile="ubuntu-23.04-desktop-amd64.iso"
	set urlfile="url=${webroot}/isos/${isofile}"
	set locales="debian-installer/locale=ja_JP.UTF-8 keyboard-configuration/layoutcode=jp keyboard-configuration/modelcode=jp106"
	set root="${webprot},${webaddr}"
	if [ "${grub_platform}" = "efi" ]; then rmmod tpm; fi
	echo "Loading ${isofile} ..."
	loopback loop (${root})${webdirs}/isos/${isofile}
	linuxefi   (loop)/casper/vmlinuz ${urlfile} ${locales} ip=dhcp ide=nodma fsck.mode=skip root=/boot boot=casper toram=minimal.standard.squashfs layerfs-path=minimal.standard.live.squashfs ---
	initrdefi  (loop)/casper/initrd
#	loopback --delete loop
}

menuentry '- Ubuntu Desktop 23.10                                         2023-10-16 11:31:22' {
	set webprot="http"
	set webaddr="192.168.1.10"
	set webdirs="/pxeboot"
	set webroot="${webprot}://${webaddr}${webdirs}"
	set isofile="ubuntu-23.10.1-desktop-amd64.iso"
	set urlfile="url=${webroot}/isos/${isofile}"
	set locales="debian-installer/locale=ja_JP.UTF-8 keyboard-configuration/layoutcode=jp keyboard-configuration/modelcode=jp106"
	set root="${webprot},${webaddr}"
	if [ "${grub_platform}" = "efi" ]; then rmmod tpm; fi
	echo "Loading ${isofile} ..."
	loopback loop (${root})${webdirs}/isos/${isofile}
	linuxefi   (loop)/casper/vmlinuz ${urlfile} ${locales} ip=dhcp ide=nodma fsck.mode=skip root=/boot boot=casper toram=minimal.standard.squashfs layerfs-path=minimal.standard.live.squashfs ---
	initrdefi  (loop)/casper/initrd
#	loopback --delete loop
}

menuentry '- Ubuntu Desktop noble                                         2023-11-27 08:01:06' {
	set webprot="http"
	set webaddr="192.168.1.10"
	set webdirs="/pxeboot"
	set webroot="${webprot}://${webaddr}${webdirs}"
	set isofile="noble-desktop-amd64.iso"
	set urlfile="url=${webroot}/isos/${isofile}"
	set locales="debian-installer/locale=ja_JP.UTF-8 keyboard-configuration/layoutcode=jp keyboard-configuration/modelcode=jp106"
	set root="${webprot},${webaddr}"
	if [ "${grub_platform}" = "efi" ]; then rmmod tpm; fi
	echo "Loading ${isofile} ..."
	loopback loop (${root})${webdirs}/isos/${isofile}
	linuxefi   (loop)/casper/vmlinuz ${urlfile} ${locales} ip=dhcp ide=nodma fsck.mode=skip root=/boot boot=casper toram=minimal.standard.squashfs layerfs-path=minimal.standard.live.squashfs ---
	initrdefi  (loop)/casper/initrd
#	loopback --delete loop
}

menuentry '- Ubuntu Legacy Desktop 23.04                                  2023-04-17 22:28:36' {
	set webprot="http"
	set webaddr="192.168.1.10"
	set webdirs="/pxeboot"
	set webroot="${webprot}://${webaddr}${webdirs}"
	set isofile="ubuntu-23.04-desktop-legacy-amd64.iso"
	set urlfile="url=${webroot}/isos/${isofile}"
	set locales="debian-installer/locale=ja_JP.UTF-8 keyboard-configuration/layoutcode=jp keyboard-configuration/modelcode=jp106"
	set root="${webprot},${webaddr}"
	if [ "${grub_platform}" = "efi" ]; then rmmod tpm; fi
	echo "Loading ${isofile} ..."
	loopback loop (${root})${webdirs}/isos/${isofile}
	linuxefi   (loop)/casper/vmlinuz ${urlfile} ${locales} ip=dhcp ide=nodma fsck.mode=skip root=/boot boot=casper toram=filesystem.squashfs maybe-ubiquity ---
	initrdefi  (loop)/casper/initrd
#	loopback --delete loop
}

menuentry '- Ubuntu Legacy Desktop 23.10                                  2023-10-10 04:57:59' {
	set webprot="http"
	set webaddr="192.168.1.10"
	set webdirs="/pxeboot"
	set webroot="${webprot}://${webaddr}${webdirs}"
	set isofile="ubuntu-23.10-desktop-legacy-amd64.iso"
	set urlfile="url=${webroot}/isos/${isofile}"
	set locales="debian-installer/locale=ja_JP.UTF-8 keyboard-configuration/layoutcode=jp keyboard-configuration/modelcode=jp106"
	set root="${webprot},${webaddr}"
	if [ "${grub_platform}" = "efi" ]; then rmmod tpm; fi
	echo "Loading ${isofile} ..."
	loopback loop (${root})${webdirs}/isos/${isofile}
	linuxefi   (loop)/casper/vmlinuz ${urlfile} ${locales} ip=dhcp ide=nodma fsck.mode=skip root=/boot boot=casper toram=filesystem.squashfs maybe-ubiquity ---
	initrdefi  (loop)/casper/initrd
#	loopback --delete loop
}

